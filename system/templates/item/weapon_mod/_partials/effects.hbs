<div class="resource-row">
	<label class="field-label">
		{{localize "FALLOUT.TEMPLATES.Damage_Effects"}}
	</label>
	<div class="grid-4-columns item-list" style="margin: 4px;">
		{{#each damageEffects}}
			<div
				{{#if (eq this.active 0)}}
					class="flexrow toggle-label"
				{{else}}
					{{#if (eq this.active 1)}}
						class="flexrow toggle-label active"
					{{else}}
						class="flexrow toggle-label disable"
					{{/if}}
				{{/if}}
				data-tooltip="{{fromConfig "DAMAGE_EFFECT_TOOLTIPS" this.key}}"
				data-active="{{this.active}}"
				data-name="{{this.key}}"
				data-type="effects"
			>
				<label style="align-self: center;">
					{{this.label}}
				</label>
				{{#if this.hasRank}}
					<input
						class="num-short-2"
						name="system.modEffects.damage.damageEffect.{{this.key}}.rank"
						type="number"
						value="{{this.rank}}"
					>
				{{/if}}
			</div>
		{{/each}}
	</div>
</div>
